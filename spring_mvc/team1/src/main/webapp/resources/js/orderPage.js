/*$.ajax({
	url: "OrderPage.do",
	success: function() {

		
		let output = "";
			output += `
            	<div class="bundleTitle">제품이름${cartDto.title}</div>
              	<div class="bundleShipping">무료배송${cartDto.shipping}</div>
	        `;

		$(".bundleItemList").html(output);
	}
});*/